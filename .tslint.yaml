defaultSeverity: error
extends:
  - tslint:recommended
  - tslint-config-prettier
rules:
  trailing-comma: never
  no-parameter-reassignment: true
  # for of causes issues in IE11 even with polyfill..
  prefer-for-of: false
